import { useState } from 'react'; import { Card, CardContent } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import { Calendar } from "@/components/ui/calendar"; import { Clock } from 'lucide-react'; import { motion } from 'framer-motion';

export default function GriffinBookingCalendar() { const [date, setDate] = useState(new Date()); const timeSlots = [ "09:00 - 13:00", "14:00 - 18:00" ];

return ( <div className="min-h-screen bg-gray-50 flex flex-col items-center justify-center p-6"> <motion.h1 className="text-3xl font-bold mb-4 text-gray-800" initial={{ opacity: 0, y: -20 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.5 }} > Griffin Fidel — Booking & Work Availability </motion.h1> <p className="text-gray-600 mb-8 text-center max-w-xl"> English Language Expert (Remote) | Braintrust AI Application<br/> Time Zone: East Africa Time (UTC +3) | Availability: 35+ Hours Weekly </p> <div className="grid md:grid-cols-2 gap-6 w-full max-w-4xl"> <Card className="shadow-md rounded-2xl"> <CardContent className="p-4 flex flex-col items-center"> <h2 className="text-xl font-semibold mb-2">Select a Date</h2> <Calendar mode="single" selected={date} onSelect={setDate} className="rounded-md border" /> </CardContent> </Card> <Card className="shadow-md rounded-2xl"> <CardContent className="p-4"> <h2 className="text-xl font-semibold mb-4">Available Time Slots</h2> {timeSlots.map((slot, index) => ( <motion.div key={index} whileHover={{ scale: 1.02 }} className="flex items-center justify-between border p-3 mb-3 rounded-xl hover:bg-blue-50 transition" > <div className="flex items-center gap-2 text-gray-700"> <Clock size={18} /> <span>{slot}</span> </div> <Button variant="outline" size="sm">Book</Button> </motion.div> ))} </CardContent> </Card> </div> <p className="text-sm text-gray-500 mt-6">For additional times or custom scheduling, please contact: griffinfidel0@gmail.com</p> </div> ); }
